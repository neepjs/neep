export {
	value, computed, isValue, encase, recover, valueify,
	markRead, markChange, safeify, exec, createExecutable,
} from 'monitorable';


export default function installMonitorable(
	api?: typeof import('monitorable'),
) {
}
